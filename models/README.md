# Model Artifacts

Run `python run_pipeline.py` to generate `baseline_ridge.joblib`, `advanced_xgboost.joblib`, and evaluation partitions. The repository may include demo artifacts, but final metrics must be regenerated from the authenticated Kaggle competition files.
